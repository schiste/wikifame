__pycache__/
*.py[cod]
*.egg-info/
.coverage
.env
.pytest_cache/
.ruff_cache/
.venv/
htmlcov/
wikifame.db
# On-wiki page drafts. The canonical copies live on-wiki; link to them, do not vendor them.
docs/onwiki/*
# The opt-out page is the exception. Its format is not prose but a contract optout.py parses,
# so the starter copy is tracked and tests/test_optout.py holds the two to each other.
!docs/onwiki/optout.fr.wiki
